<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Link-check builds only: fills History with the live test links at startup. -->
        <provider
            android:name=".linkcheck.LinkCheckSeedProvider"
            android:authorities="${applicationId}.linkcheck"
            android:exported="false" />
    </application>

</manifest>
